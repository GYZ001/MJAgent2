# Development Rules

## Priority
可运行性 > 核心正确性 > 稳定性 > 性能 > 可维护性 > 视觉效果。

## Before Coding
1. 阅读现有项目，不要假设文件和 API 存在。
2. 列出 P0、P1、P2，只实现 P0 和必要 P1。
3. 冻结依赖版本、数据结构、模块边界和关键常量。
4. 明确本次不实现的功能。
5. 未经用户确认，不得扩大需求范围。

## Implementation
- 优先修改现有代码，不得无理由重写整个项目。
- 不得自动引入新框架或大型依赖。
- 不得为了炫技加入复杂 Shader、状态机或抽象层。
- 一个问题最多评估三个方案，选择后立即实施。
- 遇到报错先定位根因，不得通过删除核心功能掩盖问题。
- 每完成一个模块就运行或测试，不要等到最后统一验证。

## Architecture and File Conventions
分层与文件规范由三道**可执行闸门**守着（都在 `py scripts/verify.py --full` 里）。动代码前先跑一遍看自己站在哪，不要凭印象。

- **先确认自己在哪一层。** `app/LAYERS.toml` 声明六层（L0 契约内核 → L5 编排入口），合法依赖只能指向层号更小或同层，`py scripts/arch_graph.py --check-layers` 报出全部上行边。**新模块必须声明层号**——未声明按 fail-safe 当 L5，会制造幻影违规把真问题淹掉：曾有 115 个未声明模块让 44 条真实倒置埋在 1,310 条报告里，补完声明后 96.6% 是幻影。
- **红线只降不升。** 两个闸门的阈值（`LAYERS.toml` 的 `max_violations`、`FILE_CONVENTIONS.toml` 的各项基线）是棘轮：拆小了就把数字调小，**不许为了让自己的改动过关而调大**。曾有人为了让一次正确的巨型函数分解通过而上调 `toplevel_defs` 基线——那说明维度本身错了（它在惩罚拆解），后来整个删掉，而不是继续放宽。
- **不得再造巨型文件。** Python ≤500 行、前端 ≤300 行、**单函数 ≤50 代码行**，`py scripts/check_file_conventions.py` 守着。这三个数取自业界通行默认值（ESLint `max-lines` 300 / SonarQube 750 之间取 500；前端 300 即 ESLint 默认值；单函数 50 是 ESLint `max-lines-per-function` 默认值，Linux CodingStyle 的「一屏」约 24-48 行），**不是从本仓库现状反推的分位数**——那种取法会让仓库越烂标准越松。单函数只数**代码行**，剔除 docstring、空行、纯注释：把文档算进复杂度就是在惩罚写文档，本仓库已因同类反向激励删过整个 `toplevel_defs` 维度。现存超标文件走 `[baseline.*]` 棘轮收敛（line_count 136 条、function_lines 319 条），**每一条基线都是欠账，不是许可**。本仓曾有 12,142 行的 `stages.py`、10,821 行的 `portraits.py`、以及 3,585 行的单函数 `compile_screenplay_ir`（比同文件第二大函数大 13.7 倍）——它们既是循环依赖的宿主，也是并行开发的冲突集中点。**装不下时先想怎么拆，不要先想加基线。**
- **拆包用真包，禁止 `exec()` 外观。** `app/api.py` / `app/worker.py` / `app/domain/` / `app/media_exec/` 曾用 `exec(compile(...), globals())` 把子文件注入自身命名空间：同一份源码两种加载方式产生两套对象，`except worker.LeaseLost` 抓不住 `run_job` 抛的同名异常，而测试全走 `worker.` 前缀所以**结构上看不见**。四个外观已全部退场，不要复活。同理禁止 `from .x import *`。
- **拆包必然让 `monkeypatch.setattr(pkg, name, stub)` 静默失效**——每个子模块持有自己的绑定，改包属性不影响它们，**测试照常变绿但验证的代码路径从未被替换**，比测试直接红危险得多。拆包时必须同步加 `tests/conftest.py` 的 `patch_<pkg>_everywhere()` 与 AST 守卫测试（仓库已有 13 个可照抄），让「漏改」从静默变成 CI 立刻报错。守卫已知盲区：循环变量形态（`for m in (a, b, pkg): setattr(m, ...)`）、以及藏在 `subprocess.run([sys.executable, "-c", ...])` 字符串字面量里的打桩。
- **助手遍历子模块要用 `sys.modules` 按全限定名解析，不要用 `getattr`。** 子模块若再导出一个与自身文件同名的符号（`from .x import x as x`），包属性会被那个符号覆盖，`getattr` 静默返回错的对象——实测曾让 `get_conn` 打桩打空、连到生产库。
- **`global` 重绑定的名字必须与它的写入者同模块。** Python 的 `global` 只重绑定**定义它的那个模块**的命名空间；分开会让写入落进私有副本、读取方永远看不到，**且不报错**。
- **拆文件后全仓 grep 硬编码路径。** `scripts/check_contract_surface.py` 的 `REQUIRED` 字典硬编码路径会让任何人跑它都 `SystemExit`；同文件 `FORBIDDEN` 字典对已删路径是 `if not path.exists(): continue`——**静默跳过，检查悄悄失效而所有人以为它在保护自己**。`tests/test_scripts_hardcoded_paths_exist.py` 现在守着这一类。

## Verification
完成后必须：
- 日常改动优先运行 `py scripts/verify.py`，只验证 Git 改动直接影响的范围；提交或发布前才运行 `py scripts/verify.py --full`；
- 运行构建或启动命令；
- 检查控制台和终端错误；
- 验证核心用户路径；
- 检查未定义引用、异步错误、边界条件和资源释放；
- 报告实际完成项、未完成项和已知限制。

禁止声称已经测试，除非确实执行了测试命令并获得结果。

**子代理禁止跑全量测试。** `pytest -q`、`pytest tests/ -q` 这类无过滤形式，以及 `scripts/verify.py`（它会把全部 Git 改动文件一起跑 ruff），子代理一律不许用；只跑与自己改动直接相关的定向测试（`-k` 过滤或点名测试文件）。全量由主会话在所有代理收工后、开一个干净的 `/tmp` worktree 统一跑。三条独立理由，任何一条都足够：

- **测的是移动靶**——并行开发时主工作区有多个代理的未提交改动，别人每落一次盘被测对象就变了，红了也分不清是谁的；
- **抢 CPU**——本机 2 核，两个全量并行会双双拖到 20 分钟以上，还拖慢用户正在跑的真实任务；
- **替代不了主会话那一轮**——主会话最终仍要在干净 worktree 上重跑，子代理这一遍纯属白烧时间。

**只写禁令挡不住，派单里必须同时给出可照抄的正面命令**（具体的 `-k` 过滤或测试文件名）——与「子代理会停下来等一个永远不到的通知」同一类教训：给动作比给禁令有效。发现已经在跑就按 PID kill（**不要 `pkill -f`**，它会匹配到自己的命令行）并发消息纠正。

**取退出码不要走管道。** `pytest ... | tail` 拿到的是 `tail` 的退出码，进程被 kill 掉也报 0；用 `pytest ... > log 2>&1; echo $?`。同族假绿还有三种，都实测遇到过：`verify.py` 不带 `--full` 只验改动范围却被描述成全量；新 worktree 缺 `data/regression_session_token.txt` 导致收集期中断、零测试执行却"16 秒跑完"；以及测试被重构悄悄削弱到不再验证它声称的东西（并发用例把线程局部的 `get_conn()` 换成共用连接，争用消失、断言恒真）。**并发测试单次绿等于没验，必须连跑 ≥10 次。**

## Workspace Hygiene
- 禁止在仓库根目录创建 `_*.py`、`_*.txt`、状态标记或临时输出；一次性诊断文件写入系统临时目录。
- 运行日志统一写入 `logs/`，可复用工具放入 `scripts/`，不得在根目录散落日志与探针。
- 历史临时产物用 `py scripts/clean_workspace.py --dry-run` 预览，再执行 `py scripts/clean_workspace.py` 清理。

## Gates and Criteria
判据必须挂在「这件事本身成没成」上，不得挂在会被正常操作改动的整体状态上。

- **挂产物信号，不挂状态字段。** `episodes.status` 白名单曾在 9+ 处重复，而调用方在闸门运行前就把 status 改坏，闸门结构上必然判不过。正例：`storyboard_pack_prompts_complete()` 只看每段 `prompt_text` 非空 + 尾镜 `is_final`。
- **比较集合用包含，不用相等。** 整集范围的素材清单会因兄弟镜的正常活动增长；精确相等会让并行任务互相打死。真正的陈旧（条目消失或内容变更）仍要 fail closed。
- **客户端持有的快照要按最小作用域拆分。** 整集级摘要会因任何一段的正常进展而自我作废，用户什么都没做错却被判过期。
- **禁止黑白名单与枚举穷举。** 判据从数据推导：合法值来自本次输入里实际存在的东西。模型返回 `cross_cut`（真实影视术语）被枚举拒绝——影视术语是开放集合，穷举不完。
- **空集合不等于「无需检查」。** `if known_ids and x not in known_ids` 在 `known_ids` 为空时短路跳过全部检查，而正确语义是「什么都不合法」。

## Ownership Must Be Explicit
凡是「用谁的连接、谁的事务、哪个供应商」这类所有权问题，一律不留默认值。

- **可选参数是缺陷的温床。** `conn=None` 让调用方漏传毫无阻力，直到线上炸出 `AttributeError`。改成必传后，漏传在调用那一刻就是 `TypeError`，测试里就能拦住。
- **不得在调用方的连接上隐式提交。** 「记一条日志」「标记一步成功」这类副作用动作若共用调用方连接并自行 `commit()`，会把未提交的中间态一起提交——本仓库已因此三次毁掉真实数据（整集分镜产出、负数 shot_no、项目级半途删除级联）。诊断类写入用独立连接；状态转移类必须显式声明用谁的事务。
- **回滚必须是异常处理器的第一条语句**，排在任何日志与 recorder 调用之前——它们各自都可能隐式提交。
- **配套参数必须一起传递。** `model` 与 `provider` 分开传会让请求打到错误端点且不报错。

## Retiring Features
废止一个功能时，为它服务的机器必须同时退场。

- 上游事实变了、下游为旧事实服务的校验没停，会变成纯粹的拦路石：首尾帧模式废止后，决定「用哪种模式」的模型调用仍在跑、仍用三种方式拦人，而它的产出在生成时被无条件丢弃。
- 质检基准跟着废止的常量走会对正确产出挑毛病（以已作废的 5 秒基准评判 15 秒产出）。
- 删除要一次删干净：删一半会让全量校验挂掉，阻塞所有并行工作。孤儿判定要覆盖动态引用形态——装饰器注册、字符串派发、Pydantic 嵌套字段类型、包装函数被内联逻辑绕过、以及 `exec()` 源码注入（`app/worker.py` 把 `media_exec/*.py` exec 进自身命名空间，那些文件的模块级名字都是对外 API）。

## User-Facing Behavior
- **界面承诺必须与实际行为一致。** 弹窗说「保留全部段落」却删了 10 段；按钮说「只生成待办」而后端整集重做；界面把 `mode_plan` 当「模型决策」展示而生成时被覆盖。任何一处不一致都要掰正，不许让界面撒谎。
- **拦住用户时必须给出路。** 提示语让用户「核对供应商任务状态」，而界面上没有任何入口能做这件事。闸门可以拦，但不能把人晾在原地。
- **不得提供「强制忽略」式绕过。** 那是把风险转嫁给用户还不告诉他——实测那笔被拦下的任务，供应商侧确实产生过费用。
- **错误要转成合适的状态码。** 走命令总线的路由会自动把 `ValueError` 转成 409；不走的路由则裸奔成 500。同一个异常在不同入口表现不一致，就是缺口。

## Prompts
- **写完整的正面陈述，不写禁令。** 只堵一种具体写法，模型换个变体照样越界：禁止某个 id 写法后，模型自造了 `character:` / `char:` / `ch:` 三种前缀。要写清合法值从哪里来、必须逐字取用、以及确实没有时该写什么。
- **模型答不出来时，先查它有没有收到标准答案。** 角色跨段换三套衣服的根因是载荷里根本没有外观字段，不是模型能力问题；接上世界书的 `appearance` 后逐字一致。
- **单次长调用会漏掉整个类别。** 一次要求模型产出多段内容时，某一类（角色、场景）可能被整体跳过。这类缺失必须有可见信号，不得静默通过。
- **不得兜底填充。** 「没识别到就沿用上一段」会制造看似正确实则编造的归属，比留空危险。空着至少是诚实的。

## Investigation
- **假设要证伪，不要顺着假设改代码。** 协调方给的假设可能全错——「分块合并丢了场景」「去重吃掉了新场景」两个假设都被实测推翻，真相是模型压根没报。顺着错假设改，会改错地方且看起来像修好了。
- **转述的结论必须自己验一遍。** 本仓库已有三次因转述失真而误判：把活代码当死代码、把「绕过」说成「拆除」、把恢复战绩安到没跑过的子系统上。
- **修复失败时先检查是不是拿猜测换了猜测。** JSON 恢复第一版把「取最后一个根」换成「按字段覆盖度择优」，打破了 7 个显式设计的 fail-closed 测试；第二版找到纯语法判据（悬空容器子元素），两边同时满足并删掉了第一版的复杂度。
- **改共享底层原语必须跑全量测试。** 子集必然漏：`model_gateway` 那次子集 586 passed，全量才炸出 7 个红，因为那个测试文件只通过公开接口间接触发被改的函数。
- **验证要有独立观察点。** 红绿验证不要回退线上代码，手写一份修复前的函数体副本来跑红；证明「真提交」要用第二条独立连接读盘上数据，而不是同一连接读自己写。
- **查不清就说查不清。** 宁可标「不确定」，也不要给一个说得通但没验证的结论。

## Destructive Operations
- **`git commit` 必须显式列路径**，禁止 `git add -A`、`git add .` 或裸 `git commit`——无参提交会把别人已暂存的改动一起扫进去。每次提交后 `git show --stat HEAD` 核对。
- **但做了 hunk 级暂存之后，提交不能再带 pathspec。** `git commit -- <path>` 会忽略索引、把该路径的完整工作区内容重新扫进去，精心拆好的部分暂存被静默抹掉。两条规则在这里冲突：整文件提交列路径，hunk 级提交靠已核验的索引（`git diff --cached` 确认后裸提交）。判据是 `git show --stat HEAD` 的行数与预期是否一致，不一致就 `reset --soft` 重做。
- **按关注点分组提交**，一个文件里混着多个关注点时拆到 hunk 级。目标是将来某组出问题能单独 revert 而不牵连其他。
- **删除或覆盖前先看目标**，列清单确认「保留项一个都不在里面」再执行。
- **破坏性操作要有原子性**：新产出确认成功之后才替换旧的，中途失败旧的必须原封不动。
- **不得用 `pkill -f "uvicorn app.main"`**——它会匹配到自己的命令行把 shell 一起杀掉。按 PID kill 再 `setsid` 重启。
- **改完代码必须手工重启后端才生效**（无托管的孤儿进程）；重启前先查在途任务，重启后核对进程启动时间晚于所有改动文件的 mtime。
- **前端不要跑 `npm run build` 当自检**，要验就 `npx tsc --noEmit` + `npx vitest run`。后端 `SpaStaticFiles` 每次请求都读盘服务 `frontend/dist`，所以写进 dist 的那一刻就是发布到生产——和后端「必须重启才生效」不对称，于是构建这个人人顺手跑的动作带上了发布副作用。2026-08-30 实测：agent 自检构建把前端发到 18:06，后端进程还停在 14:50，用户拿到新前端配旧后端，小说导入预检直接挂掉。vite 的 outDir 现已指向 `frontend/dist-staging`（构建无副作用），发布走 `py scripts/publish_frontend.py`：它校验产物完整性、拦住「后端启动时间早于最新 `app/**/*.py`」的版本偏斜（并给出重启命令），再原子替换。`tests/test_frontend_publish_gate.py` 守着 outDir 不被改回 `dist`。
