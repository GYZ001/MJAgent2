"""分镜脚本业务校验器 V1~V8（docs/PROMPT_SPEC.md §C）。
错误消息必须具体到字段与数值——修复回路把它们逐条回喂模型（1.0 教训：从不告诉模型哪里错了）。

这是一个真包（非 exec 命名空间外观）：每个子模块是普通 Python 模块，彼此用显式
`from .x import y` 互相依赖。下面按子模块显式再导出每一个符号（含每个子模块自己
`import` 进来的名字，不只是它原生定义的——`app/stages/__init__.py` 拆包时就是这个
口径），保证 `from app.validators import name` / `app.validators.name` 与拆分前
逐一对应，包括测试直接 import 的私有名。用 `name as name` 显式再导出（PEP 484 的
显式重导出写法），而不是给 pyproject.toml 加 per-file-ignore——本次拆分的文件
归属只包含 app/validators.py 与 app/validators/**，不改 pyproject.toml。禁止
`from .x import *`（见 app/FILE_CONVENTIONS.toml 的 star_import 闸门）。新增校验
逻辑请加进对应关注点的子模块，不要加回本文件。
"""
from __future__ import annotations

from .primitives import (
    SAME_SCENE_CONTINUITY_MODES as SAME_SCENE_CONTINUITY_MODES,
    SCENE_CUT_TRANSITIONS as SCENE_CUT_TRANSITIONS,
    SOURCE_EXCERPT_MIN_CHARS as SOURCE_EXCERPT_MIN_CHARS,
    Shot as Shot,
    Storyboard as Storyboard,
    TRANSITIONS as TRANSITIONS,
    _LEADING_ACTION_SEQUENCE_RE as _LEADING_ACTION_SEQUENCE_RE,
    _named_character_is_explicitly_offscreen as _named_character_is_explicitly_offscreen,
    _normalize_scene_label as _normalize_scene_label,
    _normalized_spoken_text as _normalized_spoken_text,
    _scene_time_changed as _scene_time_changed,
    _scene_time_key as _scene_time_key,
    _shot_capacity_budget_total as _shot_capacity_budget_total,
    _too_similar as _too_similar,
    adjacent_spoken_repeat_errors as adjacent_spoken_repeat_errors,
    default_scene_transition as default_scene_transition,
    difflib as difflib,
    normalize_action_desc as normalize_action_desc,
    normalize_transition_visuals as normalize_transition_visuals,
    re as re,
    same_scene as same_scene,
    scene_time_of as scene_time_of,
    storyboard_shot_count_range as storyboard_shot_count_range,
    sys as sys,
)
from .scene_match import (
    Any as Any,
    Bible as Bible,
    EpisodeScreenplay as EpisodeScreenplay,
    Shot as Shot,
    Storyboard as Storyboard,
    StoryboardOutline as StoryboardOutline,
    _normalize_scene_label as _normalize_scene_label,
    _scene_contiguity_key as _scene_contiguity_key,
    _scene_label_variants as _scene_label_variants,
    _screenplay_scene_resolution_errors as _screenplay_scene_resolution_errors,
    _storyboard_scene_contiguity_key as _storyboard_scene_contiguity_key,
    canonicalize_storyboard_scene as canonicalize_storyboard_scene,
    compose_scene_setting as compose_scene_setting,
    difflib as difflib,
    match_scene_name as match_scene_name,
    re as re,
    resolve_screenplay_scene_sequence as resolve_screenplay_scene_sequence,
    scene_name_of as scene_name_of,
    split_legacy_scene_setting as split_legacy_scene_setting,
    validate_storyboard_outline_scene_alignment as validate_storyboard_outline_scene_alignment,
    validate_storyboard_scenes as validate_storyboard_scenes,
    validate_storyboard_screenplay_scene_alignment as validate_storyboard_screenplay_scene_alignment,
)
from .screenplay_text import (
    EpisodeScreenplay as EpisodeScreenplay,
    KEY_CONTENT_MAX_REPORT as KEY_CONTENT_MAX_REPORT,
    KEY_LINES_MIN as KEY_LINES_MIN,
    KEY_LINE_BIGRAM_COVERAGE as KEY_LINE_BIGRAM_COVERAGE,
    KEY_LINE_PRESENT_RATIO as KEY_LINE_PRESENT_RATIO,
    KEY_PLOT_POINTS_MIN as KEY_PLOT_POINTS_MIN,
    KEY_POINT_COVERAGE as KEY_POINT_COVERAGE,
    KeyDialogueChain as KeyDialogueChain,
    MIN_KEY_LINES as MIN_KEY_LINES,
    MIN_KEY_PLOT_POINTS as MIN_KEY_PLOT_POINTS,
    ORDER_CHECK_MIN_CORE_CHARS as ORDER_CHECK_MIN_CORE_CHARS,
    SCRIPT_DIALOGUE_LINE_RE as SCRIPT_DIALOGUE_LINE_RE,
    SCRIPT_SCENE_HEADING_RE as SCRIPT_SCENE_HEADING_RE,
    SCRIPT_SOUND_LINE_RE as SCRIPT_SOUND_LINE_RE,
    _CLAIM_SPLIT_RE as _CLAIM_SPLIT_RE,
    _NON_CONTENT_RE as _NON_CONTENT_RE,
    _SCRIPT_GLUED_HEADING_DIALOGUE_RE as _SCRIPT_GLUED_HEADING_DIALOGUE_RE,
    _SOURCE_PREFIXED_UTTERANCE_RE as _SOURCE_PREFIXED_UTTERANCE_RE,
    _SOURCE_QUOTED_UTTERANCE_RE as _SOURCE_QUOTED_UTTERANCE_RE,
    _SPEAKER_PREFIX_RE as _SPEAKER_PREFIX_RE,
    _atomize_claim as _atomize_claim,
    _bigram_coverage as _bigram_coverage,
    _bigram_set as _bigram_set,
    _condense as _condense,
    _dialogue_chain_crosses_hard_scene_boundary as _dialogue_chain_crosses_hard_scene_boundary,
    _iter_script_sound_matches as _iter_script_sound_matches,
    _key_lines_from_chains as _key_lines_from_chains,
    _longest_run_ratio as _longest_run_ratio,
    _matching_text_indices as _matching_text_indices,
    _screenplay_scene_space as _screenplay_scene_space,
    _script_dialogue_turns as _script_dialogue_turns,
    _speaker_name as _speaker_name,
    _strip_speaker as _strip_speaker,
    _structured_key_line_functions as _structured_key_line_functions,
    derive_key_lines as derive_key_lines,
    key_line_order_errors as key_line_order_errors,
    key_lines_in_story_order as key_lines_in_story_order,
    re as re,
    screenplay_speaker_names as screenplay_speaker_names,
    source_dialogue_fragments as source_dialogue_fragments,
    textmatch as textmatch,
)
from .dialogue_chains import (
    Any as Any,
    DIALOGUE_CHAIN_TURNS_HARD_MAX as DIALOGUE_CHAIN_TURNS_HARD_MAX,
    EpisodeScreenplay as EpisodeScreenplay,
    KEY_LINE_BIGRAM_COVERAGE as KEY_LINE_BIGRAM_COVERAGE,
    KEY_LINE_PRESENT_RATIO as KEY_LINE_PRESENT_RATIO,
    KeyDialogueChain as KeyDialogueChain,
    MIN_KEY_LINES as MIN_KEY_LINES,
    SCRIPT_SOUND_LINE_RE as SCRIPT_SOUND_LINE_RE,
    _DIALOGUE_RESPONSE_FUNCTIONS as _DIALOGUE_RESPONSE_FUNCTIONS,
    _DIALOGUE_TURN_FUNCTIONS as _DIALOGUE_TURN_FUNCTIONS,
    _bigram_coverage as _bigram_coverage,
    _condense as _condense,
    _dialogue_chain_crosses_hard_scene_boundary as _dialogue_chain_crosses_hard_scene_boundary,
    _is_grounded_short_utterance as _is_grounded_short_utterance,
    _longest_run_ratio as _longest_run_ratio,
    _matching_text_indices as _matching_text_indices,
    _script_dialogue_turns as _script_dialogue_turns,
    align_source_excerpt as align_source_excerpt,
    chunk_dialogue_turns as chunk_dialogue_turns,
    config as config,
    content_char_count as content_char_count,
    key_lines_in_story_order as key_lines_in_story_order,
    normalize_screenplay_dialogue_chains as normalize_screenplay_dialogue_chains,
    re as re,
    textmatch as textmatch,
    validate_dialogue_chains as validate_dialogue_chains,
)
from .ending_hook import (
    Any as Any,
    COVERS_ATOM_ABSENT_COVERAGE as COVERS_ATOM_ABSENT_COVERAGE,
    COVERS_ATOM_ABSENT_RUN as COVERS_ATOM_ABSENT_RUN,
    ENDING_HOOK_EVENT_COVERAGE as ENDING_HOOK_EVENT_COVERAGE,
    ENDING_HOOK_GROUNDING_COVERAGE as ENDING_HOOK_GROUNDING_COVERAGE,
    ENDING_HOOK_WINDOW_COVERAGE as ENDING_HOOK_WINDOW_COVERAGE,
    ENDING_HOOK_WINDOW_MAX_EVENTS as ENDING_HOOK_WINDOW_MAX_EVENTS,
    ENDING_HOOK_WINDOW_MIN_CONTRIBUTORS as ENDING_HOOK_WINDOW_MIN_CONTRIBUTORS,
    ENDING_HOOK_WINDOW_MIN_STRONG_CONTRIBUTION as ENDING_HOOK_WINDOW_MIN_STRONG_CONTRIBUTION,
    KEY_POINT_COVERAGE as KEY_POINT_COVERAGE,
    StoryEvent as StoryEvent,
    _bigram_coverage as _bigram_coverage,
    _bigram_set as _bigram_set,
    _claim_clearly_absent as _claim_clearly_absent,
    _ending_hook_eligible_events as _ending_hook_eligible_events,
    _ending_hook_event_core_text as _ending_hook_event_core_text,
    _ending_hook_event_text as _ending_hook_event_text,
    _ending_hook_window_match as _ending_hook_window_match,
    _longest_run_ratio as _longest_run_ratio,
    _strip_speaker as _strip_speaker,
    ending_hook_grounding_report as ending_hook_grounding_report,
    ending_hook_is_grounded as ending_hook_is_grounded,
)
from .screenplay_ledger import (
    DELIVERY_OWNERS as DELIVERY_OWNERS,
    EpisodeScreenplay as EpisodeScreenplay,
    InformationItem as InformationItem,
    SPINE_BEATS_MIN as SPINE_BEATS_MIN,
    StoryEvent as StoryEvent,
    _bigram_coverage as _bigram_coverage,
    defaultdict as defaultdict,
    index_source_segments as index_source_segments,
    normalize_screenplay_candidate as normalize_screenplay_candidate,
    normalize_screenplay_dialogue_chains as normalize_screenplay_dialogue_chains,
    normalize_screenplay_ledgers as normalize_screenplay_ledgers,
    re as re,
    validate_plot_spine as validate_plot_spine,
    validate_screenplay_source_coverage as validate_screenplay_source_coverage,
)
from .prep_pack_ledger import (
    Any as Any,
    PARATEXT_TAIL_WINDOW_SEGMENTS as PARATEXT_TAIL_WINDOW_SEGMENTS,
    PREP_PACK_SPAN_LAZINESS_MULTIPLIER as PREP_PACK_SPAN_LAZINESS_MULTIPLIER,
    SourceSegment as SourceSegment,
    _PREP_PACK_QUOTE_MIN_MATCH_CHARS as _PREP_PACK_QUOTE_MIN_MATCH_CHARS,
    _prep_pack_deterministic_title_indexes as _prep_pack_deterministic_title_indexes,
    _prep_pack_structural_paratext_indexes as _prep_pack_structural_paratext_indexes,
    align_source_excerpt as align_source_excerpt,
    assert_prep_pack_coverage_complete as assert_prep_pack_coverage_complete,
    assert_prep_pack_span_union_matches_ledger as assert_prep_pack_span_union_matches_ledger,
    build_prep_pack_span_ledger as build_prep_pack_span_ledger,
    chapter_title_segment_indexes as chapter_title_segment_indexes,
    index_source_segments as index_source_segments,
    structural_front_matter_ids as structural_front_matter_ids,
)
from .storyboard_delivery import (
    EpisodeScreenplay as EpisodeScreenplay,
    KEY_CONTENT_MAX_REPORT as KEY_CONTENT_MAX_REPORT,
    KEY_LINE_BIGRAM_COVERAGE as KEY_LINE_BIGRAM_COVERAGE,
    KEY_LINE_PRESENT_RATIO as KEY_LINE_PRESENT_RATIO,
    KEY_POINT_COVERAGE as KEY_POINT_COVERAGE,
    Shot as Shot,
    Storyboard as Storyboard,
    StoryboardOutline as StoryboardOutline,
    _atomize_claim as _atomize_claim,
    _bigram_coverage as _bigram_coverage,
    _claim_clearly_absent as _claim_clearly_absent,
    _condense as _condense,
    _iter_script_sound_matches as _iter_script_sound_matches,
    _longest_run_ratio as _longest_run_ratio,
    _screenplay_sound_stats as _screenplay_sound_stats,
    _speaker_name as _speaker_name,
    _spine_delivery_clauses as _spine_delivery_clauses,
    _spine_receptive_claim as _spine_receptive_claim,
    _strip_speaker as _strip_speaker,
    config as config,
    content_char_count as content_char_count,
    key_line_catalog as key_line_catalog,
    key_line_delivery_errors as key_line_delivery_errors,
    key_line_order_errors as key_line_order_errors,
    key_lines_in_story_order as key_lines_in_story_order,
    max_speech_chars as max_speech_chars,
    outline_key_line_capacity_errors as outline_key_line_capacity_errors,
    re as re,
    spoken_text_of as spoken_text_of,
    validate_spine_delivery_ledger as validate_spine_delivery_ledger,
    validate_storyboard_preserves_key_content as validate_storyboard_preserves_key_content,
    validate_storyboard_shot_covers_outline as validate_storyboard_shot_covers_outline,
    validate_storyboard_soundtrack as validate_storyboard_soundtrack,
)
from .screenplay_validate import (
    Bible as Bible,
    DELIVERY_OWNERS as DELIVERY_OWNERS,
    EpisodeScreenplay as EpisodeScreenplay,
    KEY_CONTENT_MAX_REPORT as KEY_CONTENT_MAX_REPORT,
    KEY_LINE_BIGRAM_COVERAGE as KEY_LINE_BIGRAM_COVERAGE,
    KEY_LINE_PRESENT_RATIO as KEY_LINE_PRESENT_RATIO,
    MIN_KEY_LINES as MIN_KEY_LINES,
    MIN_KEY_PLOT_POINTS as MIN_KEY_PLOT_POINTS,
    PlotSpineBeat as PlotSpineBeat,
    SCENE_OUTLINE_MIN as SCENE_OUTLINE_MIN,
    SCENE_STORY_FUNCTION_MIN_CHARS as SCENE_STORY_FUNCTION_MIN_CHARS,
    SCRIPT_DIALOGUE_LINE_RE as SCRIPT_DIALOGUE_LINE_RE,
    SCRIPT_SCENE_HEADING_RE as SCRIPT_SCENE_HEADING_RE,
    _DIALOGUE_RESPONSE_FUNCTIONS as _DIALOGUE_RESPONSE_FUNCTIONS,
    _bigram_coverage as _bigram_coverage,
    _claim_clearly_absent as _claim_clearly_absent,
    _condense as _condense,
    _iter_script_sound_matches as _iter_script_sound_matches,
    _longest_run_ratio as _longest_run_ratio,
    _matching_text_indices as _matching_text_indices,
    _script_dialogue_turns as _script_dialogue_turns,
    _speaker_name as _speaker_name,
    _spine_delivery_clauses as _spine_delivery_clauses,
    _spine_receptive_claim as _spine_receptive_claim,
    _strip_speaker as _strip_speaker,
    _structured_key_line_functions as _structured_key_line_functions,
    adaptation_hook_errors as adaptation_hook_errors,
    is_functional_extra as is_functional_extra,
    key_line_order_errors as key_line_order_errors,
    math as math,
    re as re,
    screenplay_speaker_names as screenplay_speaker_names,
    textmatch as textmatch,
    typed_functional_identity_names as typed_functional_identity_names,
    validate_dialogue_chains as validate_dialogue_chains,
    validate_plot_spine as validate_plot_spine,
    validate_screenplay as validate_screenplay,
    validate_screenplay_source_coverage as validate_screenplay_source_coverage,
    validate_screenplay_spine_delivery as validate_screenplay_spine_delivery,
)
from .storyboard_core import (
    ACTION_DESC_HARD_MIN as ACTION_DESC_HARD_MIN,
    ACTION_DESC_TARGET_MAX as ACTION_DESC_TARGET_MAX,
    ACTION_DESC_TARGET_MIN as ACTION_DESC_TARGET_MIN,
    Any as Any,
    Bible as Bible,
    CAMERA_MOVES as CAMERA_MOVES,
    CONTINUITY_MODES as CONTINUITY_MODES,
    DURATION_REVIEW_RISK_TAG as DURATION_REVIEW_RISK_TAG,
    EpisodeScreenplay as EpisodeScreenplay,
    HUMAN_DURATION_REVIEW_TAG as HUMAN_DURATION_REVIEW_TAG,
    NarrativeContinuityPlan as NarrativeContinuityPlan,
    PREFERRED_SHOT_DURATION_S as PREFERRED_SHOT_DURATION_S,
    SAME_SCENE_CONTINUITY_MODES as SAME_SCENE_CONTINUITY_MODES,
    SCENE_CUT_TRANSITIONS as SCENE_CUT_TRANSITIONS,
    SHOT_SIZES as SHOT_SIZES,
    SOURCE_EXCERPT_MIN_CHARS as SOURCE_EXCERPT_MIN_CHARS,
    Shot as Shot,
    Storyboard as Storyboard,
    TRANSITIONS as TRANSITIONS,
    _named_character_is_explicitly_offscreen as _named_character_is_explicitly_offscreen,
    _scene_time_changed as _scene_time_changed,
    _shot_capacity_budget_total as _shot_capacity_budget_total,
    _storyboard_scene_contiguity_key as _storyboard_scene_contiguity_key,
    _too_similar as _too_similar,
    action_capacity_errors as action_capacity_errors,
    adjacent_spoken_repeat_errors as adjacent_spoken_repeat_errors,
    config as config,
    count_sequential_action_beats as count_sequential_action_beats,
    dialogue_focus_subject as dialogue_focus_subject,
    dialogue_two_shot_required as dialogue_two_shot_required,
    duration_gt5_errors as duration_gt5_errors,
    implicit_speech_without_dialogue_errors as implicit_speech_without_dialogue_errors,
    is_allowed_storyboard_character as is_allowed_storyboard_character,
    is_functional_extra as is_functional_extra,
    is_system_environment_entity_id as is_system_environment_entity_id,
    narrative_action_capacity_profile as narrative_action_capacity_profile,
    normalize_action_desc as normalize_action_desc,
    scene_name_of as scene_name_of,
    scene_time_of as scene_time_of,
    shot_count_budget_errors as shot_count_budget_errors,
    shot_duration_should_prefer_five as shot_duration_should_prefer_five,
    shot_id_space_errors as shot_id_space_errors,
    speech_capacity_errors as speech_capacity_errors,
    spoken_chars_from_shot as spoken_chars_from_shot,
    spoken_contract_coherence_errors as spoken_contract_coherence_errors,
    spoken_speakers as spoken_speakers,
    state_chain_errors as state_chain_errors,
    storyboard_pack_dialogue_errors as storyboard_pack_dialogue_errors,
    sync_shot_continuity_fields as sync_shot_continuity_fields,
    typed_functional_identity_names as typed_functional_identity_names,
    validate_storyboard as validate_storyboard,
    validate_storyboard_pack_dialogue as validate_storyboard_pack_dialogue,
    validate_storyboard_scenes as validate_storyboard_scenes,
)
from .outline_dialogue import (
    Any as Any,
    EpisodeScreenplay as EpisodeScreenplay,
    KEY_LINE_BIGRAM_COVERAGE as KEY_LINE_BIGRAM_COVERAGE,
    KEY_LINE_PRESENT_RATIO as KEY_LINE_PRESENT_RATIO,
    KEY_POINT_COVERAGE as KEY_POINT_COVERAGE,
    StoryboardOutline as StoryboardOutline,
    _OUTLINE_SPEECH_CUE_RE as _OUTLINE_SPEECH_CUE_RE,
    _bigram_coverage as _bigram_coverage,
    _condense as _condense,
    _longest_run_ratio as _longest_run_ratio,
    _outline_dialogue_two_shot_required as _outline_dialogue_two_shot_required,
    _speaker_name as _speaker_name,
    _split_outline_text_outside_quotes as _split_outline_text_outside_quotes,
    _strip_speaker as _strip_speaker,
    assign_outline_delivery_ids as assign_outline_delivery_ids,
    config as config,
    content_char_count as content_char_count,
    key_line_catalog as key_line_catalog,
    max_speech_chars as max_speech_chars,
    normalize_outline_dialogue_ownership as normalize_outline_dialogue_ownership,
    normalize_outline_spoken_durations as normalize_outline_spoken_durations,
    outline_key_line_speaker_errors as outline_key_line_speaker_errors,
    re as re,
    split_outline_on_speaker_changes as split_outline_on_speaker_changes,
)
from .outline_capacity import (
    Any as Any,
    Bible as Bible,
    EpisodeScreenplay as EpisodeScreenplay,
    NarrativeContinuityPlan as NarrativeContinuityPlan,
    StoryboardOutline as StoryboardOutline,
    _ACTION_CAPACITY_SPLIT_MARKER as _ACTION_CAPACITY_SPLIT_MARKER,
    _atomize_claim as _atomize_claim,
    _normalize_scene_label as _normalize_scene_label,
    _outline_action_candidate as _outline_action_candidate,
    _split_outline_action_text as _split_outline_action_text,
    _split_outline_text_outside_quotes as _split_outline_text_outside_quotes,
    _strip_speaker as _strip_speaker,
    action_capacity_errors as action_capacity_errors,
    action_capacity_limit as action_capacity_limit,
    config as config,
    content_char_count as content_char_count,
    count_sequential_action_beats as count_sequential_action_beats,
    key_line_catalog as key_line_catalog,
    match_scene_name as match_scene_name,
    max_speech_chars as max_speech_chars,
    narrative_outline_action_capacity_errors as narrative_outline_action_capacity_errors,
    outline_scene_coverage_errors as outline_scene_coverage_errors,
    split_legacy_scene_setting as split_legacy_scene_setting,
    split_outline_over_action_capacity as split_outline_over_action_capacity,
    split_outline_over_key_line_capacity as split_outline_over_key_line_capacity,
)
from .outline_validate import (
    Bible as Bible,
    EpisodeScreenplay as EpisodeScreenplay,
    KEY_CONTENT_MAX_REPORT as KEY_CONTENT_MAX_REPORT,
    KEY_LINE_BIGRAM_COVERAGE as KEY_LINE_BIGRAM_COVERAGE,
    KEY_LINE_PRESENT_RATIO as KEY_LINE_PRESENT_RATIO,
    KEY_POINT_COVERAGE as KEY_POINT_COVERAGE,
    Shot as Shot,
    Storyboard as Storyboard,
    StoryboardOutline as StoryboardOutline,
    _bigram_coverage as _bigram_coverage,
    _condense as _condense,
    _longest_run_ratio as _longest_run_ratio,
    _strip_speaker as _strip_speaker,
    _too_similar as _too_similar,
    key_line_catalog as key_line_catalog,
    key_line_order_errors as key_line_order_errors,
    key_lines_in_story_order as key_lines_in_story_order,
    outline_atomic_errors as outline_atomic_errors,
    outline_key_line_capacity_errors as outline_key_line_capacity_errors,
    outline_key_line_speaker_errors as outline_key_line_speaker_errors,
    outline_scene_coverage_errors as outline_scene_coverage_errors,
    score_storyboard_direction_readability as score_storyboard_direction_readability,
    shot_count_budget_errors as shot_count_budget_errors,
    validate_storyboard_direction_contract as validate_storyboard_direction_contract,
    validate_storyboard_outline as validate_storyboard_outline,
    validate_storyboard_outline_scene_alignment as validate_storyboard_outline_scene_alignment,
)
from .continuity_gate import (
    DURATION_REVIEW_RISK_TAG as DURATION_REVIEW_RISK_TAG,
    EpisodeScreenplay as EpisodeScreenplay,
    HUMAN_DURATION_REVIEW_TAG as HUMAN_DURATION_REVIEW_TAG,
    NarrativeContinuityPlan as NarrativeContinuityPlan,
    PREFERRED_SHOT_DURATION_S as PREFERRED_SHOT_DURATION_S,
    RULE_SPOKEN_COHERENCE as RULE_SPOKEN_COHERENCE,
    Shot as Shot,
    Storyboard as Storyboard,
    _retime_coherent_spoken_timeline as _retime_coherent_spoken_timeline,
    _shot_capacity_budget_total as _shot_capacity_budget_total,
    action_capacity_errors as action_capacity_errors,
    action_capacity_limit as action_capacity_limit,
    build_timeline_from_segments as build_timeline_from_segments,
    config as config,
    count_sequential_action_beats as count_sequential_action_beats,
    information_ledger_errors as information_ledger_errors,
    narrative_action_capacity_profile as narrative_action_capacity_profile,
    normalize_board_continuity as normalize_board_continuity,
    normalize_continuity as normalize_continuity,
    prefer_default_shot_durations as prefer_default_shot_durations,
    relieve_spoken_overflow as relieve_spoken_overflow,
    segments_from_timeline as segments_from_timeline,
    shot_duration_should_prefer_five as shot_duration_should_prefer_five,
    speech_capacity_errors as speech_capacity_errors,
    spoken_char_count as spoken_char_count,
    spoken_chars_from_shot as spoken_chars_from_shot,
    state_chain_errors as state_chain_errors,
    strip_all_narration as strip_all_narration,
    sync_shot_continuity_fields as sync_shot_continuity_fields,
    validate_spoken_contract as validate_spoken_contract,
    validate_storyboard_continuity_contract as validate_storyboard_continuity_contract,
)
from .offbible_characters import (
    Bible as Bible,
    Shot as Shot,
    Storyboard as Storyboard,
    _CHARACTER_REFERENCE_PREFIXES as _CHARACTER_REFERENCE_PREFIXES,
    _CHARACTER_TEXT_FIELDS as _CHARACTER_TEXT_FIELDS,
    _canonical_bible_name as _canonical_bible_name,
    _dedupe_names as _dedupe_names,
    _rename_shot_character as _rename_shot_character,
    _replace_character_mention as _replace_character_mention,
    _strip_shot_character_contract as _strip_shot_character_contract,
    is_collective_role as is_collective_role,
    is_functional_extra as is_functional_extra,
    normalize_offbible_characters as normalize_offbible_characters,
    re as re,
    synchronize_spoken_contract as synchronize_spoken_contract,
)
from .bible_validate import (
    Bible as Bible,
    re as re,
    validate_bible as validate_bible,
    validate_scene_bible as validate_scene_bible,
)
