"""支付骨架：微信支付（Native）/ 支付宝（PC 网站支付）下单、回调验签、对账、发货。

⚠️ **本包当前不可上线收款。** 两个前提未满足，缺一不可：

1. **密码学是手写实现，未经审计**（``crypto_rsa.py``/``crypto_asn1.py``/
   ``crypto_aesgcm.py``，共约 470 行）。本环境未装 ``cryptography``，按派单的
   降级方案用纯标准库实现；已用 GCM 规范原始论文的官方 Test Case 验证过正确
   性，但"通过了我能构造的全部测试"与"可以拿去处理真钱"是两回事。真实收款前
   必须换成 ``cryptography``。
2. **所有出站调用从未打过真实端点**（下单、主动查单）——没有商户号与密钥，
   做不到端到端验证。结构完整、密码学自洽，但端到端未验证。

上线前的完整清单见 ``docs`` 与验收报告；在这两条解决之前，任何"看起来能跑"
都不构成可以收钱的证据。

范围与边界（见 2026-08-30 派单，`git log` 同日 "先尝试做微信和支付宝的支付方式"）：

- 本包只做**结构正确、幂等安全、可测**的订单与回调机制，不承诺已用真实商户号
  验证过端到端链路——本环境没有微信/支付宝商户号与密钥，无法做到。
- 发货复用已有的 ``app.quota_addon.grant_video_addon_seconds``（加量包）与直接
  写 ``users.tier``（档位升级，和 ``app.auth.admin_api.update_user`` 现有的管理员
  改档位路径同一种写法——档位本来就没有 ledger，不是本包发明了新记账方式）。
  本包不重新实现配额记账，只按 ``attempt_key=订单号`` 调用既有函数。
- 签名校验：微信 V3 平台证书验 ``Wechatpay-Signature``，支付宝公钥验 ``sign``，
  都用纯标准库实现（RSA-SHA256/PKCS1v1.5，通过 ``pow()`` 模幂验签，见
  ``crypto_asn1.py``/``crypto_rsa.py``）——环境未装 ``cryptography``，按派单
  "没有就用标准库能做的方案并说明限制" 处理。微信回调的 ``resource`` 字段是
  AEAD (AES-256-GCM) 加密的，读取通知内容（订单号/金额/交易状态）离不开解密，
  因此 ``crypto_aesgcm.py`` 额外手写了 AES-256 + GCM（仅需加密方向，GCM 的
  CTR/GHASH 都只用 AES 正向），已用 GCM 规范原始论文 Appendix B 的三组官方
  Test Case（13/14/16，NIST/McGrew-Viega）验证正确性，但**不是审计过的实现**，
  上线前应换成 ``cryptography`` 库，见模块文档。

明确不做（写进验收报告，不在这里重复）：退款、发票、真实商户配置、前端支付页。
"""
from __future__ import annotations
